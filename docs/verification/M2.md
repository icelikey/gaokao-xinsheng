# M2 Verification Log

## Scope

Data model, first question-bank admin preview and import dry-run contract.

## Current Status

Partially verified locally on 2026-06-29.

This is not a real Supabase dev deployment yet. It proves that the MVP question-bank shape, admin preview surfaces, import dry-run contract and catalog API contract exist locally.

## Evidence

- Shared MVP paper fixture exists at `packages/contracts/src/mvp-paper.ts`.
- Fixture contains:
  - 1 preview paper: `paper_2010_sd_math_sci_mvp`
  - 30 math questions
  - choice, fill-blank and free-response question types
  - rubric preview data for free-response questions
- Admin question-bank entry exists at `apps/web/src/app/admin/page.tsx`.
- Paper list page exists at `apps/web/src/app/admin/papers/page.tsx`.
- Question preview page exists at `apps/web/src/app/admin/papers/[paperId]/page.tsx`.
- Paper import dry-run page exists at `apps/web/src/app/admin/papers/import/page.tsx`.
- Admin import API exists at `POST /api/v1/admin/paper-imports`.
- Import batches are recorded in memory mode and mapped to Supabase `paper_import_batches` plus `audit_logs` in Supabase mode.
- Accepted imports use the explicit `replace_preview` conflict policy and commit paper, section, question, answer-key and rubric rows in memory/dev backends.
- `supabase/migrations/202606290006_paper_import_batches.sql` defines the admin-only import batch evidence table with conflict policy, commit summary, RLS enabled and no direct anon/authenticated grants.
- Read-only catalog APIs exist:
  - `GET /api/catalog/papers`
  - `GET /api/catalog/papers/paper_2010_sd_math_sci_mvp`
- v1 paper-matching catalog APIs exist:
  - `GET /api/v1/catalog/years`
  - `GET /api/v1/catalog/regions?year=2010`
  - `POST /api/v1/catalog/match`
- `paper_mapping_rules` migration and seed exist for 2010届山东理科数学.
- `pnpm test:paper-import` proves a valid 30-question MVP payload dry-runs as `VALIDATED`, non-dry-run imports as `IMPORTED`, commits 30 question rows, sums to 150 points, counts answer keys/rubric questions, and rejects duplicate order plus total-score mismatch.
- `pnpm test:catalog-contract` proves the MVP mapping returns exactly one public candidate and unknown regions do not match.
- `pnpm verify` completed successfully after this change:
  - ESLint passed with `--max-warnings=0`.
  - contracts typecheck passed.
  - web typecheck passed.
  - Next build generated 8 app routes.
- Browser verification captured:
  - page: `http://127.0.0.1:3000/admin/papers/paper_2010_sd_math_sci_mvp`
  - H1: `2010届山东理科数学MVP精选卷`
  - rendered question rows: `30`
  - first question: `集合A={x|x>1}, B={x|x<4}, A∩B为`
- Screenshot saved at `output/admin-question-preview-screenshot.png`.
- Import page screenshot saved at `output/admin-paper-import-screenshot.png`.
- Local import API smoke after restarting the dev server:

```json
{
  "ok": true,
  "status": 201,
  "importStatus": "VALIDATED",
  "questionCount": 30,
  "maxScoreSum": 150,
  "issueCount": 1,
  "errorIssues": 0,
  "error": null
}
```

The single issue is a non-blocking warning for no `multiple_choice` questions in the MVP sample; `errorIssues=0`.

Local accepted-import API smoke after restarting the dev server:

```json
{
  "ok": true,
  "status": 201,
  "importStatus": "IMPORTED",
  "conflictPolicy": "replace_preview",
  "committed": true,
  "questionRows": 30,
  "answerKeyRows": 30,
  "rubricRows": 8,
  "errorIssues": 0,
  "error": null
}
```

## Remaining M2 Work

- Run `supabase/migrations/202606290001_initial_schema.sql` against a real Supabase dev project.
- Run `supabase/migrations/202606290004_paper_mapping_rules.sql` against a real Supabase dev project.
- Run `supabase/migrations/202606290006_paper_import_batches.sql` against a real Supabase dev project.
- Run `supabase/seed/seed.sql` against the same dev project.
- Prove the hosted `paper_mapping_rules` seed maps 2010届山东理科数学 to `paper_2010_sd_math_sci_mvp`.
- Prove `POST /api/v1/admin/paper-imports` writes `paper_import_batches`, `audit_logs`, `papers`, `sections`, `questions`, `answer_keys` and `grading_rubrics` in hosted Supabase dev.
- Replace local fixture-backed admin reads with server API reads from Supabase.
- Add rubric review state machine and real manual score adjustment audit flow.
- Define RLS and service-role-only boundaries before any user-facing write access.
