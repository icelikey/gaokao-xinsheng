# M4 Verification Log

## Scope

Local AI tutor loop: L1-L4 static hints, pre-AI answer snapshot, leak detection and golden-set verification.

## Current Status

Partially verified locally on 2026-06-29.

This is not a real model gateway yet. It proves the first cost-controlled and auditable hint path before adding live AI providers.

## Evidence

- Reusable AI tutor package exists at `packages/ai-tutor`.
- Web API imports the AI tutor package from `apps/web/src/lib/exam-store.ts`.
- `requestAiHelp` keeps the pre-AI snapshot behavior:
  - if the user already answered, it references the latest answer version
  - if the user has not answered, it creates an empty answer snapshot before returning a hint
- AI tutor output includes:
  - `message`
  - `knowledgePoints`
  - `suggestedNextAction`
  - `revealsFinalAnswer`
  - `safetyFlags`
- Golden-set script exists at `scripts/ai-golden-check.ts`.
- Root verification now includes `pnpm test:ai-golden`.
- `pnpm test:ai-golden` result:

```json
{
  "ok": true,
  "paperId": "paper_2010_sd_math_sci_mvp",
  "questions": 30,
  "checkedHints": 60,
  "leakedHints": 0
}
```

- API smoke test for `POST /api/v1/exam-sessions/:sessionId/ai-help` returned:
  - `level`: `2`
  - `message`: `把已知条件转成一个更熟悉的数学对象，再用排除法检查选项是否满足题意。`
  - `knowledgePoints`: `审题`, `选项排除`
  - `suggestedNextAction`: `写出可用公式或关键关系。`
  - `revealsFinalAnswer`: `false`
  - `safetyFlags`: `PRE_AI_VERSION_REQUIRED`
  - non-empty `preAiAnswerVersionId`
- `pnpm verify` completed successfully after this change:
  - ESLint passed with `--max-warnings=0`.
  - contracts typecheck passed.
  - ai-tutor typecheck passed.
  - web typecheck passed.
  - golden-set leak check passed.
  - Next build passed.

## Remaining M4 Work

- Add model-provider adapters behind the same AI tutor contract.
- Store Prompt/model versions in Supabase.
- Persist AI help events in Supabase with audit metadata.
- Expand the golden set with adversarial prompts and question-specific expected boundaries.
- Add a reviewer workflow for rejected or suspicious hints.
