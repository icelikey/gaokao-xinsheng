# M1 Verification Log

## Scope

Project skeleton and cloud-first development plan.

## Evidence To Collect

- `pnpm install` succeeds.
- `pnpm verify` succeeds.
- `supabase/migrations/202606290001_initial_schema.sql` exists and can be reviewed.
- Product preview image exists at `output/gaokao-xinsheng-product-preview.png`.
- GitHub Actions workflow exists at `.github/workflows/ci.yml`.

## Current Status

Verified locally on 2026-06-29.

## Evidence

- `pnpm install` completed successfully after workspace build-script approvals were declared for `sharp` and `unrs-resolver`.
- `pnpm verify` completed successfully:
  - ESLint passed with `--max-warnings=0`.
  - `@gaokao-xinsheng/contracts` typecheck passed.
  - `@gaokao-xinsheng/web` typecheck passed.
  - `next build` compiled and generated the app routes.
- Local dev server responded at `http://127.0.0.1:3000`.
- Health API returned:

```json
{"service":"gaokao-xinsheng-web","status":"ok","environment":"development"}
```

- Browser screenshot captured at `output/web-home-screenshot.png`.
- Product preview image exists at `output/gaokao-xinsheng-product-preview.png` and is copied into `apps/web/public/preview/gaokao-xinsheng-product-preview.png`.
- Initial Supabase schema exists at `supabase/migrations/202606290001_initial_schema.sql`.
- MVP seed file contains 30 math questions at `supabase/seed/seed.sql`.
- GitHub Actions workflow exists at `.github/workflows/ci.yml`.

## Remaining M1 Cloud Steps

- Connect the repository to GitHub.
- Connect GitHub to Vercel and confirm PR Preview URLs.
- Create Supabase dev/staging/prod projects or equivalent branches.
- Run the migration against a real Supabase dev project.
