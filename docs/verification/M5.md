# M5 Verification Log

## Scope

Local grading loop: grading package, deterministic objective scoring, fill-blank equivalence, rubric-based free-response scoring, review signal and grading job shape.

## Current Status

Partially verified locally on 2026-06-29.

This is not a real Supabase Queue or Edge Function worker yet. It proves the scoring contract and the local job/report shape that later queue workers should preserve.

## Evidence

- Reusable grader package exists at `packages/grader`.
- Web submit flow imports the grader from `apps/web/src/lib/exam-store.ts`.
- Report generation now grades two answer sets:
  - independent score from the first pre-AI answer snapshot per question
  - collaborative score from the final submitted answer
- Grading job query API exists:
  - `GET /api/v1/grading-jobs/:jobId`
- Root verification now includes `pnpm test:grading-golden`.
- `pnpm test:grading-golden` result:

```json
{
  "ok": true,
  "paperId": "paper_2010_sd_math_sci_mvp",
  "checkedQuestions": 30,
  "score": 15,
  "reviewCount": 1,
  "normalizationSample": "3x^2"
}
```

- HTTP API smoke test proved:
  - submit generated `gradingJobId`
  - grading job state returned `SUCCEEDED`
  - `q_2010_sd_math_17` accepted `3*x^2` as equivalent to `3x^2`
  - free-response `q_2010_sd_math_19` scored through rubric
  - free-response result retained `requiresReview=true`
  - report `reviewCount=1`
- Manual score adjustment API exists:
  - `POST /api/v1/admin/review-queue/:reportId/score-adjustments`
  - validates score is within `0..maxScore`
  - updates the per-question collaborative score
  - recalculates report collaborative score
  - records before/after score in the review audit log

Local manual score-adjustment HTTP smoke after restarting the dev server:

```json
{
  "ok": true,
  "reportId": "report_7ee0674a-2746-44a1-a50a-f0a7350be314",
  "questionId": "q_2010_sd_math_19",
  "beforeScore": 2,
  "afterScore": 5,
  "adjustedQuestionScore": 5,
  "adjustedRequiresReview": false,
  "collaborativeBefore": 7,
  "collaborativeAfter": 10,
  "auditAction": "ADJUST_SCORE",
  "healthMode": "memory"
}
```
- `pnpm verify` completed successfully after this change:
  - ESLint passed with `--max-warnings=0`.
  - contracts typecheck passed.
  - ai-tutor typecheck passed.
  - grader typecheck passed.
  - web typecheck passed.
  - AI golden-set leak check passed.
  - grading golden-set check passed.
  - Next build passed and included `/api/v1/grading-jobs/[jobId]`.
- `pnpm test:store-backend` now includes a dual-score regression:

```json
{
  "ok": true,
  "memoryBackendCreatesSession": true,
  "memoryBackendKeepsPreAiSnapshot": true,
  "memoryBackendDualScoreDiff": 5,
  "memoryReviewWorkflowAudited": true,
  "memoryScoreAdjustmentAudited": true,
  "memoryReviewQueueRemovesResolved": true,
  "supabaseBackendFailsClosed": true,
  "supabasePendingCode": "SUPABASE_STORE_ADAPTER_PENDING"
}
```

## Remaining M5 Work

- Replace local synchronous grading with a Supabase Queue-backed worker.
- Persist grading jobs and grading results in Supabase.
- Store independent per-question grading results as durable rows instead of recomputing them from answer versions.
- Store rubric score details in `grading_results.evidence`.
- Prove low-confidence reviewer state transitions against hosted Supabase dev data.
- Add model-assisted second-pass scoring behind the same grader contract.
